# Jev 规则设计

[English](DESIGN_en.md) | 简体中文

## 数据与执行

DSH settings 拥有持久规则。每条规则包含稳定 ID、启停、可编辑规则标题和说明、事件、内容来源、自定义内容、题目来源、题目标题或脚本、阈值，以及 2–16 个含稳定 ID、文案和动作的选项。旧 `phase/question/context/threshold` 规则在 settings 读取时迁移；旧 `beforeEnabled/afterEnabled` 字段仅保持读取兼容。运行时以已启用规则为准。

事件 `before` 在 `agent/pre-step` 读取最新用户消息或当前步骤可见文本，并在主模型请求前追加选中选项的上下文。`after` 在 `llm/stream` 看见宿主工具结果后读取工具 ID、失败状态和文字，缓冲原始流，判定后在 `finish` 前追加选中选项的提醒。此阶段不能撤回已显示内容。自定义内容作为固定文字在对应事件发生时使用。

题目生成脚本是用户在设置中保存的受信任 JavaScript，运行于独立 Node 工作线程，限时 5 秒、内存限额 64 MiB。它可以用 Node API 读取文件；它接收事件、待判断内容、选项 ID 与默认文案，只能返回标题和相同 ID 的文案。策略解释器仅执行持久规则中的动作，绝不从模型输出或脚本输出读取动作。

普通模型请求一次返回各规则各选项的 JSON 概率，严格验证 ID、范围和总和。最高概率选项达到阈值后，解释器按顺序应用动作，并以总上下文预算约束追加文字。失败路径保留原始决定或流。Jev 原生型号由模型 ID 识别，目前在提供商适配前拒绝普通 JSON 假冒。

## 界面与测试

规则页仅提供总开关、判定模型和四段式规则编辑。规则标题与说明分别保存并在收起状态显示；旧规则缺标题时显示序号。一次只展开一条规则，长帮助与选项动作参数默认折叠，保存操作位于规则列表上方；每个动态来源可查看实际读取的内容，脚本模式明确显示运行时生成。选项内配置动作类型和参数。测试页是设置分区内的独立视图，用按钮进入和返回，运行一次不修改会话的选择题。普通模型展示各选项概率和分布集中度，不把它称为校准置信度。

## 验收

验证旧规则迁移、settings 浏览器序列化、输入来源、脚本文件读取与输出约束、各选项动作、概率与预算、构建、固定提交安装、已装包、DSH 新进程和浏览器真实交互。
