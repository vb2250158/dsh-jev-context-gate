# Jev 上下文门禁设计

[English](DESIGN_en.md) | 简体中文

状态：实现中；安装和端到端验收以目标环境的运行证据为准。

## 所有权

独立 DSH 插件，无 RabiRoute、Manager、人格或计划运行依赖。源码在独立 Git 仓库；用户设置由 DSH settings 管理，发布按固定 Git 提交安装。官方源码仅作合同核对，不作为新插件源码位置。

## 数据驱动

规则数据包含稳定 id、启停、before/after 阶段、完整判定问题、命中概率阈值、预设注入内容。按数组顺序应用，限制总上下文长度，不截断单条指令。模型只能返回规则判定，不能提供任意执行脚本、扩大工具权限或替换注入模板。规则解释器由运行和测试面板共用。

## 前置

用户提交后、主模型首个请求前，判定是否需要调查及相关工具/Skill 指引。仅在规则命中时注入。工具指引不是工具权限，也不能令未安装工具变为可用。需要检查 DSH 公开工具组成合同后，才决定是否支持动态工具装配。

## 后置

以用户实际请求、候选答复和宿主记录的工具执行结果为输入，检测未经调查的归因，以及用“可能”措辞替代本可执行调查的行为。主模型自述不是调查凭证；文件被读取也不等于结论得到证实。

必须核对后置事件与流式显示的先后关系。若停止事件发生在文字已经显示后，只能称为后置纠偏，不能宣传为发送前拦截。不得修改历史。纠偏次数有界，尊重用户取消、权限拒绝和实际阻碍。

## 模型

DSH provider/model 是判定模型的身份来源，不能改变对话主模型。型号以 `jev-` 开头时自动识别为 Jev 原生，其余型号识别为 LLM JSON 模拟；旧 `nativeJev` 设置只作兼容读取，不决定模式。Jev 原生结构化协议需由模型提供商按官方合同接入，不能把普通聊天 JSON 当作 Jev 原生响应。普通模型输出的选项概率是估计值；界面用归一化熵计算分布集中度，明确标为模拟置信度，不能称为校准 confidence。无有效结果、超时和配置变化有明确状态，不暗中改投其他模型。

## 设置与测试

DSH 设置增加 Jev 菜单：总开关、模型、前后开关、规则编辑、预算与纠偏上限。复用现有模型目录和受支持的模型选择器入口；无法原样复用时说明边界，不导入私有组件。

“Jev 测试”先支持一道 Choice 题：输入待判断文本、题目和 2–16 个选项，普通模型通过宿主测试路由执行一次受限 JSON 判定，显示选项概率、最高概率选项、模拟置信度、耗时与错误。测试不向真实会话注入或执行工具。Jev 型号明确标记为原生；在模型提供商暴露结构化接口前，测试按钮不可用。测试输入不会自动加入公开仓库或共享配置。

## 验收

结构化判定器与纯规则验证、宿主生命周期与取消/重入测试、真实组合加载、设置保存冲突、模型选择不影响主会话、测试面板真实模型调用、明暗及自定义主题、卸载释放、公开包脱敏和固定提交安装。当前已通过规则解释器六项和结构化判定器四项单元测试；宿主组合、真实模型面板、后置证据约束和安装验收仍未完成。
