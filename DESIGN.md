# Jev 规则设计

[English](DESIGN_en.md) | 简体中文

## 数据与执行

DSH settings 拥有持久规则。每条规则包含稳定 ID、启停、可编辑规则标题和说明、事件、内容来源、自定义内容、题目来源、题目标题或脚本、阈值和选项来源。手动选项有 2–16 项，每项保存稳定 ID、文案和动作；动态选项保存来源、筛选目标、N 和筛选后的行为。旧 `phase/question/context/threshold` 规则在 settings 读取时迁移；旧 `beforeEnabled/afterEnabled` 字段仅保持读取兼容。运行时以已启用规则为准。

事件 `before` 在 `agent/pre-step` 读取最新用户消息或当前步骤可见文本，并在主模型请求前追加选中选项的上下文。`after` 在 `llm/stream` 看见宿主工具结果后读取工具 ID、失败状态和文字，缓冲原始流，判定后在 `finish` 前追加选中选项的提醒。此阶段不能撤回已显示内容。自定义内容作为固定文字在对应事件发生时使用。

`tool-before` 和 `tool-after` 直接订阅宿主工具管线。规则保存确切工具名或前缀、待判断内容、题目、阈值和选项行为；调用前的拒绝返回宿主 `deny`，调用后的上下文通过 `additionalContexts` 进入下一次请求。群行为调用已安装的 Rabi 发送／查询工具，使用规则配置的精确目标与稳定投递 ID；内部调用不再触发同一 Jev 规则。群答复等待由可取消计时器驱动，当前进程终止时结束。群目标未配置时无法启用规则。

前置选项可请求注入指定名称的 Skill。Jev 通过当前 Agent 作用域和工作目录加载模型可调用的 Skill，在写入会话前触发 `skill-injection` 规则事件。事件的判断内容由规则选择：Skill 简介与最新用户消息、Skill 正文、最新用户消息、当前步骤文本或固定文字；题目、选项和阈值不写死。选项行为可继续、跳过当前候选 Skill，或在 Skill 正文后附加上下文。若配置了此事件却无法完成判定，本次注入不执行。已在可见会话中记录的完整 Skill 正文不重复写入；用户显式调用 Skill 由宿主处理，不受此事件影响。

`skill-catalog` 事件由规则配置驱动。内置的“Skill 裁剪”规则用当前可见会话文字作输入，选择事件参数 `skills` 作为候选项，默认保留评分前 10 项。事件与参数定义分别提供稳定 `key` 和可本地化的 `display`；规则保存参数 `key`，界面只显示 `display`。判定模型一次接收全部候选，为每项返回独立的 0–1 相关度分数，按规则的筛选目标取前 N、后 N、超过 N% 或低于 N% 的项。裁剪行为同步改写目录消息正文和持久来源条目；额外内容行为保留目录，另注入配置文字。目录规则失败时保留原消息并记日志。宿主将已发布的目录记录在会话历史中；旧会话此前的目录不会被追溯删除。

`before` 事件也可配置候选筛选。候选来源为按换行、空格或指定字符串拆分的待判断内容、逐行固定列表，或受信任脚本返回的文字数组／`{ id, text }` 数组。共同的评分器接收规则的待判断内容、题目与全部候选项，一次请求返回每项评分；不设固定的 100 项分批上限，提供商输入／输出容量仍是实际限制。规则可选择裁剪或注入额外内容，入选内容组成一条插件上下文消息，受已有上下文预算限制；原用户消息保持原样，失败不注入。即使候选数量小于前 N 数量，也必须评分并应用配置的最低评分。

题目生成脚本是用户在设置中保存的受信任 JavaScript，运行于独立 Node 工作线程，限时 5 秒、内存限额 64 MiB。它可以用 Node API 读取文件；它接收事件、待判断内容、本次选项 ID 与默认文案，可以返回标题及相同 ID 的文案。策略解释器仅执行持久规则中的动作，绝不从模型输出或脚本输出读取动作。

普通模型请求一次返回各规则各选项的 JSON 概率，严格验证 ID、范围和总和。最高概率选项达到阈值后，解释器按顺序应用动作，并以总上下文预算约束追加文字。失败路径保留原始决定或流。Jev 原生型号由模型 ID 识别，目前在提供商适配前拒绝普通 JSON 假冒。

## 界面与测试

规则页提供总开关、判定模型和规则编辑弹窗。规则标题与说明分别保存并在列表显示；旧规则缺标题时显示说明。动态候选显示来源、事件参数显示名、分割方式、筛选目标与行为；普通规则继续在选项内配置动作类型和参数。测试页是设置分区内的独立视图，用按钮进入和返回，运行一次不修改会话的选择题。普通模型展示各选项概率和分布集中度，不把它称为校准置信度。

## 验收

验证旧规则迁移、settings 浏览器序列化、输入来源、脚本文件读取与输出约束、各选项动作、概率与预算、构建、固定提交安装、已装包、DSH 新进程和浏览器真实交互。
